{
  "name": "safe-net",
  "version": "1.0.0",
  "description": "Safe-Net Monorepo Project",
  "private": true,
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "type-check": "turbo run type-check",
    "db:generate": "pnpm --filter @repo/database db:generate",
    "db:migrate": "pnpm --filter @repo/database db:migrate",
    "db:push": "pnpm --filter @repo/database db:push",
    "db:studio": "pnpm --filter @repo/database db:studio",
    "db:seed": "pnpm --filter @repo/database db:seed",
    "db:reset": "pnpm --filter @repo/database db:reset",
    "update-ip": "node scripts/get-local-ip.js",
    "show-ip": "node scripts/show-network-info.js",
    "check-backend": "node scripts/check-backend.js",
    "test:alert": "tsx scripts/create-test-alert.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "tsx": "^4.21.0",
    "turbo": "^2.7.4",
    "typescript": "^5.3.3"
  },
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  }
}
