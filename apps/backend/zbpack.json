{
  "$schema": "https://zeabur.com/schemas/zbpack.json",
  "build_command": "pnpm install && pnpm build && npx prisma generate",
  "start_command": "npx prisma migrate deploy && pnpm start:prod",
  "dockerfile": null,
  "env": {
    "NODE_ENV": "production",
    "PORT": "3001"
  }
}
