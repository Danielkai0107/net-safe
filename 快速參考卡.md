# 🎯 Safe-Net 網路配置快速參考卡

## 🔥 切換 WiFi 後（30 秒解決）

```bash
pnpm update-ip
# 然後重啟前端（如果正在運行）
```

---

## 🛠️ 常用命令

| 命令 | 功能 | 使用時機 |
|------|------|----------|
| `pnpm update-ip` | 更新 IP 配置 | 切換網路後 |
| `pnpm show-ip` | 查看網路資訊 | 檢查配置狀態 |
| `pnpm check-backend` | 檢測後端連接 | 前端連不上時 |

---

## 📱 啟動服務

```bash
# 後端
cd apps/backend && pnpm dev

# Admin 前端
cd apps/admin && pnpm dev

# Mobile App
cd apps/mobile && pnpm android
```

---

## 🎯 三種解決方案

### 1️⃣ 自動 IP（當前）
```bash
pnpm update-ip  # 切換網路後執行
```

### 2️⃣ mDNS（一次設定）
```typescript
// 使用電腦名稱，不會變
API_BASE_URL = 'http://DanieldeMacBook-Air.local:3001/api'
```

### 3️⃣ 雲端部署（一勞永逸）
```bash
npm i -g @railway/cli
cd apps/backend
railway login && railway init && railway up
```

---

## 🆘 遇到問題？

```bash
# 診斷
pnpm check-backend

# 查看配置
pnpm show-ip

# 測試 API
curl http://localhost:3001/api/health
```

---

## 📚 詳細文件

- `QUICK_FIX_NETWORK.md` - 快速修復
- `網路問題終極解決方案.md` - 完整方案
- `DEPLOY_GUIDE.md` - 雲端部署

---

## 💡 當前配置

```
IP: 192.168.1.107
Admin: http://192.168.1.107:3001/api
Mobile: http://192.168.1.107:3001/api
狀態: ✅ 已配置
```

---

**列印此卡片貼在螢幕旁！** 📌
